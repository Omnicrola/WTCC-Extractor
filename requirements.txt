# WTCC Extraction Pipeline Dependencies

# Audio transcription with speaker diarization
whisperx
--extra-index-url https://download.pytorch.org/whl/cu126
torch==2.8.0+cu126
torchvision==0.23.0+cu126
torchaudio==2.8.0+cu126

# LLM structured extraction via LM Studio local server
# Requires LM Studio running with a model loaded and the local server started.
# Download LM Studio: https://lmstudio.ai
openai
pydantic>=2.0

# Fuzzy string matching for phonetic deduplication (05_data_cleanup.py)
rapidfuzz

# ffmpeg must be installed separately and on PATH:
#   Windows: winget install ffmpeg   OR   choco install ffmpeg
#   macOS:   brew install ffmpeg
#   Linux:   sudo apt install ffmpeg

# audfprint must be cloned separately (not on PyPI):
#   git clone https://github.com/dpwe/audfprint
#   pip install -r audfprint/requirements.txt
#   Then set AUDFPRINT_PY in config.py

# audfprint dependencies (install these regardless):
numpy
scipy
librosa
matplotlib
